#!/usr/bin/env node

import { App } from '../index.js';

(async function init() {
	const app = new App();
	await app.init();
	// Parse ARGV and run
	app.run(process.argv.slice(2)).catch((e) => console.warn(e.stack));
})();
