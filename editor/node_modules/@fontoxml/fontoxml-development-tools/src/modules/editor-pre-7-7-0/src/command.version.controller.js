export default async function editorVersionCommand(req, res) {
	let format = req.options.format;
	if (format === true) {
		format = 'full';
	}
	res.raw(req.fdt.editorRepository.sdkVersion.format(format));
}
