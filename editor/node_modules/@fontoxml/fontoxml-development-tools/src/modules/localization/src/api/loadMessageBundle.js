import fs from 'fs-extra';

import Message from './Message.js';

/**
 * @param  {string}  path
 *
 * @return  {Promise.<Message[]>}
 */
export default function loadMessageBundle(path) {
	return fs.readJson(path).then((messages) =>
		messages.map((msg) => Message.fromJSON(msg)),
	);
}
