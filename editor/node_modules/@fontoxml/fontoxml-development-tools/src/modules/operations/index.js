import commandOperation from './src/command.operation.js';
import commandOperations from './src/command.operations.js';

export default async (moduleRegistration) => {
	await commandOperations(moduleRegistration);
	await commandOperation(moduleRegistration);
};
