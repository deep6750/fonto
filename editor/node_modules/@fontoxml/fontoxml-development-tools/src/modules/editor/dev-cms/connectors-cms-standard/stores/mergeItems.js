export default function mergeItems(...lists) {
	const itemById = lists.reduce((byId, list) => {
		if (list) {
			for (const item of list) {
				byId[item.id] = item;
			}
		}
		return byId;
	}, {});

	return Object.keys(itemById).map((id) => itemById[id]);
}
