/**
 * configureHealthGetRouteHandler
 *
 * @return {function(req, res, next)}
 */
export default function configureHealthGetRouteHandler() {
	return (_req, res, _next) => {
		res.status(200).json({ ok: true });
	};
}
