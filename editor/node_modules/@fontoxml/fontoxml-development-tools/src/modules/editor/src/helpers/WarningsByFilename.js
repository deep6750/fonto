export default class WarningsByFilename extends Map {
	add(filename, warning) {
		let warnings = this.get(filename);
		if (!warnings) {
			warnings = [];
			this.set(filename, warnings);
		}

		warnings.push(warning);
	}
};
