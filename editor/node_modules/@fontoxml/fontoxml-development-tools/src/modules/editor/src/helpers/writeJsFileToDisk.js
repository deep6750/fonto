import fs from 'fs-extra';

/**
 * Write the output file to disk.
 *
 * @param {Object} file
 *
 * @return {Promise}
 */
export default async function writeJsFileToDisk(file) {
	await fs.outputFile(file.filePath, file.code);

	return file;
}
